<!DOCTYPE html>
<html lang="ko-kr">
<head>
  <title>Bootstrap Example</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="<%= customCssDomain %>">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.0/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  <style>
    /* Set height of the grid so .sidenav can be 100% (adjust if needed) */
    .row.content {height: 1500px}
    
    /* Set gray background color and 100% height */
    .sidenav {
      background-color: #f1f1f1;
      height: 100%;
    }
    
    /* Set black background color, white text and some padding */
    footer {
      background-color: #555;
      color: white;
      padding: 15px;
    }
    
    /* On small screens, set height to 'auto' for sidenav and grid */
    @media screen and (max-width: 767px) {
      .sidenav {
        height: auto;
        padding: 15px;
      }
      .row.content {height: auto;} 
    }
  </style>

  <script>

    $( document ).ready(function() {
        //$( ".newpage" ).load( "README.md.html" );
        
        // $('#page-wrapper').html('<% content %>');

        let a1 = $('.content').height();
        let a2 = $('.col-sm-9').height();
        if(a1 < a2)$('.content').height(a2);
        // $('.nav').children.addClass('active');
        // class="active"
    });

  </script>
</head>
<body>

<div class="container-fluid">
  <div class="row content">
    <div class="col-sm-3 sidenav">
      <h3>미래먹거리-nxth.io</h3>
      <ul class="nav nav-pills nav-stacked">
        <% for(var i=0; i<list.length; i++) {%>
            <li><a href="/docs/<%= list[i] %>"><%= list[i] %></a></li>
        <% } %>
        <% if(swaggerDoc) { %>
        <li><a href="/docs/swagger?url=/swagger.json">swagger</a></li>
        <% } %>
      </ul><br>
    </div>
    <div class="col-sm-9" id="page-wrapper">
        <%- content %>
    </div>
  </div>
</div>
<footer class="container-fluid">
  <p>Footer Text</p>
</footer>
</body>
</html>
